<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :Autor %><br />
    <%= f.text_field :commenter %>
  </div>
  <div class="field">
    <%= f.label :Contenido %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
